package com.uniTienda.Repository.Orden;
import org.springframework.data.jpa.repository.JpaRepository;

import com.uniTienda.Model.Orden.PSE;

public interface PSERepository extends JpaRepository<PSE, Long> {
}